name: Deploy to EC2

on:
  push:
    branches: [ feature/deployment ]

permissions:
  id-token: write
  contents: read
  pull-requests: write

env:
  AWS_REGION: us-east-1
  EC2_INSTANCE_DNS: ec2-54-90-67-241.compute-1.amazonaws.com
  REMOTE_USER: ec2-user
  SECRET_NAME: prod/ec2/ssh-key

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: 'arn:aws:iam::398306423585:role/github-actions-role'
          aws-region: ${{ env.AWS_REGION }}
          audience: sts.amazonaws.com

      - name: Get SSH key from Secrets Manager
        run: |
          mkdir -p ~/.ssh
          aws secretsmanager get-secret-value --secret-id ${{ env.SECRET_NAME }} \
            --query 'SecretString' --output text > ~/.ssh/logistics_api.pem
          chmod 600 ~/.ssh/logistics_api.pem

      - name: Setup SSH
        run: |
          ssh-keyscan -H ${{ env.EC2_INSTANCE_DNS }} >> ~/.ssh/known_hosts
          eval `ssh-agent -s`
          ssh-add ~/.ssh/logistics_api.pem
      
      - name: Create deployment package
        run: |
          tar -czf app.tar.gz ./* 
            
      - name: Copy files to EC2
        run: |
            ssh ${{ env.REMOTE_USER }}@${{ env.EC2_INSTANCE_IP }} 'mkdir -p ~/app'
            scp app.tar.gz ${{ env.REMOTE_USER }}@${{ env.EC2_INSTANCE_IP }}:~/app/
            ssh ${{ env.REMOTE_USER }}@${{ env.EC2_INSTANCE_IP }} '
            cd ~/app
            tar -xzf app.tar.gz
            rm app.tar.gz
            '

      - name: Deploy application
        run: |
            ssh ${{ env.REMOTE_USER }}@${{ env.EC2_INSTANCE_IP }} '
                cd ~/app
                sudo docker-compose down
                sudo docker-compose build --no-cache api
                sudo docker-compose up -d
            '